/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['4sirteameyesonly.djarumfoundation.org'],
        unoptimized: true,
    },
}

module.exports = nextConfig
